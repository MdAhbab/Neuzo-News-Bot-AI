# Neuzo Agentic Newsbot - Dependencies
# Kept intentionally lean: every package below is imported by the backend.

# Backend API
flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0

# Database
mysql-connector-python>=8.2.0

# Security
bcrypt>=4.0.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# News fetching and crawling
requests>=2.31.0
newsapi-python>=0.2.7
feedparser>=6.0.10
beautifulsoup4>=4.12.0

# NLP verification (pulls in torch + transformers as its own dependencies)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Document Generation
python-docx>=0.8.11

# Optional: Redis session storage (only used when REDIS_URL is set)
redis>=5.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
